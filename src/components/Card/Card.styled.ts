import styled from "styled-components";

export const Card = styled.div`
  display: inline;
  margin: 10px;
  padding: 10px;
  border-radius: 20px;
  background: var(--gray);
  
  & img {
    width: 200px;
    height: 200px;
    border-radius: 20px;
  }
`
